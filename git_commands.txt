#INITIALIZATION
git config --global user.name "username"	#enter your name
git config --global user.email "email"		#enter your email

git config --get user.name			#check your name	
git config --get user.email			#check your email

it config --global init.defaultBranch main

ls ~/.ssh/id_ed25519.pub	
ssh-keygen -t ed25519 -C <email address>	#DON'T include <> in command
cat ~/.ssh/id_ed25519.pub			#reading the ssh key
ssh -T git@github.com                   	#testing the connection

#COMMANDS
git status
git pull
git commit -m "SomeMessage"
git clone git@github.com:protoncoder/git_test.git
git remote -v 					#
git log
git push origin main
git add . 					#add all changed files to stagger area
git config --global core.editor "code --wait"   #fix for vscode when entering <git commit> only




#USEFUL INFO
The basic Git syntax is program | action | destination.

For example,

    git add . is read as git | add | ., where the period represents everything in the current directory;
    git commit -m "message" is read as git | commit -m | "message"; and
    git status is read as git | status | (no destination).



